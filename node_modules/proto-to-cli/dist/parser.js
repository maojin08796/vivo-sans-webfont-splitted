'use strict';

Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });

const fs = require('fs-extra');

const toInt = (value, dummyPrevious) => {
  const parsedValue = parseInt(value, 10);
  if (isNaN(parsedValue)) {
    throw new Error(dummyPrevious + "Not a number.");
  }
  return parsedValue;
};
const toFloat = (value, dummyPrevious) => {
  const parsedValue = parseFloat(value);
  if (isNaN(parsedValue)) {
    throw new Error(dummyPrevious + "Not a number.");
  }
  return parsedValue;
};
const toFile = (value, dummyPrevious) => {
  return new Uint8Array(fs.readFileSync(value).buffer);
};
const toBoolean = (value, dummyPrevious) => {
  if (typeof value === "string") {
    if (value.toLocaleLowerCase() === "true") {
      return true;
    }
    if (value.toLocaleLowerCase() === "false") {
      return false;
    }
    return true;
  }
  return value;
};
const HandleRepeated = (fn) => (value, dummyPrevious) => {
  return value.split(",").map((v) => v.trim()).map((i) => fn(i, dummyPrevious[0]));
};

exports.HandleRepeated = HandleRepeated;
exports.toBoolean = toBoolean;
exports.toFile = toFile;
exports.toFloat = toFloat;
exports.toInt = toInt;
//# sourceMappingURL=parser.js.map
